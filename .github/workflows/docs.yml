name: docs

on:
  push:
    branches:
      - 20**
      - furo
    paths:
      - "src/**"
      - "docs/source/**"
      - ".github/workflows/docs.yml"
      - "pyproject.toml"
  pull_request:
    branches:
      - 20**
    types: [opened, reopened, synchronize]
    paths:
      - "src/**"
      - "docs/source/**"
      - ".github/workflows/docs.yml"
      - "pyproject.toml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sphinx:
    if: >
      github.event_name == 'push' ||
      github.event_name == 'release' ||
      github.event_name == 'workflow_dispatch' ||
      github.base_ref == '2023' ||
      contains(github.event.pull_request.labels.*.name, 'conflicts') ||
      contains(github.event.pull_request.labels.*.name, 'docs-test')
    runs-on: ubuntu-latest
    strategy:
      matrix:
        language: ["en", "zh_CN"]
        builder: ["html"]
        theme: ["sphinx_rtd_theme"]
    env:
      SPHINX_THEME: ${{ matrix.theme }}
      REPO_NAME: ${{ github.event.repository.name }}
      LANGUAGE: ${{ matrix.language }}
      VERSION: ${{ github.ref_name && github.event_name != 'pull_request' || github.base_ref }}
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Installing the library
        run: |
          pip install .[docs]
          pip show abqpy
      - name: Build the documentation
        run: |
          sphinx-build -b changes -j auto -W docs/source docs/build/changes
          sphinx-build -M ${{ matrix.builder }} ${{ env.SPHINXOPTS }} docs/source docs/build/${{ matrix.builder }}
        env:
          SPHINXOPTS: -D language=${{ matrix.language }} -j auto -W
      - name: Upload the documentation
        if: github.event_name != 'pull_request' && matrix.builder == 'html'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/build/${{ matrix.builder }}
          destination_dir: ${{ matrix.language }}/${{ env.VERSION }}
          keep_files: true
